# Server Configuration
PORT=3000

# Worker API Authentication
WORKER_API_KEY=your-secure-api-key-here

# Remote Worker Configuration (for local worker connecting to remote API)
API_BASE_URL=https://your-deployed-app.onrender.com

# Email Configuration (Proton Mail SMTP)
SMTP_HOST=smtp.protonmail.ch
SMTP_PORT=587
SMTP_USER=your-email@protonmail.com
SMTP_PASS=your-smtp-password
